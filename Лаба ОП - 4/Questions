Контрольные вопросы

В чем разница между списком, словарем и кортежем?
    Список - аналог массива, изменяемый тип данных
    Словарь - аналог ассоциативного массива, изменяемый тип данных
    Кортеж - аналог массива, неизменяемый тип данных
Как добавить элемент в конце списка? в середину?
    Метод append(): Он позволяет добавить элемент в конец списка.
    Метод extend(): Он позволяет добавить элемент (и даже целый другой список) в конец списка.
    Метод insert(): Метод insert похож на append, но только первым параметром указывается индекс элемента,
                    перед которым будет вставка. Для вставки в середину: list.insert(len(list)//2, element)
Как удалить элемент с конца списка? из середины?
    Метод pop удаляет элемент по индексу и возвращает его. Если не указывать параметры, то будет удален последний элемент.
Каково назначение операции in ? len ? clear ? Какие еще операции можно применять к структурным данным языка Python?
    in - проверка наличия
    len - длина структуры
    clear - уничтожение данных структуры (очистка)
    structure[index] = value - присваивание по индексу
    + - конкатенация
    * - дублирование
    structure[index_first:index_last] - использование среза для доступа к элементам
Какие математические операции над множествами поддерживает язык Python?
    len(s) - число элементов в множестве (размер множества).
    x in s - принадлежит ли x множеству s.
    set.isdisjoint(other) - истина, если set и other не имеют общих элементов.
    set == other - все элементы set принадлежат other, все элементы other принадлежат set.
    set.issubset(other) или set <= other - все элементы set принадлежат other.
    set.issuperset(other) или set >= other - аналогично.
    set.union(other, ...) или set | other | ... - объединение нескольких множеств.
    set.intersection(other, ...) или set & other & ... - пересечение.
    set.difference(other, ...) или set - other - ... - множество из всех элементов set, не принадлежащие ни одному из other.
    set.symmetric_difference(other); set ^ other - множество из элементов, встречающихся в одном множестве, но не встречающиеся в обоих.
    set.copy() - копия множества.